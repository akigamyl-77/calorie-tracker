import React from 'react';
import ReactDOM from 'react-dom/client';
import CalorieTrackerDashboard from './CalorieTracker';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <CalorieTrackerDashboard />
  </React.StrictMode>
);
